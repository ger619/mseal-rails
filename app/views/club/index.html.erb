
  <div class="p-8 bg-[#FAE115] font-bold uppercase text-5xl">
    CLUB & HISTORY| <%=link_to "Add Club History", new_club_path, class: "w-[290px] h-[53px] bg-[black] font-normal my-3 py-3 text-2xl text-white" %>
  </div>
<% @clubs.each do |club| %>
  <div class="flex flex-col gap-4 flex flex-row-reverse md:flex-row  items-center border-t-2  border-[#FAE115] my-4 bg-[#fdfee9] p-10">
    <div class="flex flex-col ">
      <h1 class="md:text-4xl text-3xl uppercase mt-5 font-normal mb-8">
        <%= club.header %>
      </h1>
      <p class="md:w-[928px] font-light text-xl">
        <%= club.body %>
      </p>

      <%= link_to club_path(club.id) do %>
      <button class="w-[290px] h-[53px] bg-[#F4E721] font-normal my-3 py-3 text-2xl ">
        Read More
      </button>
      <% end %>
    </div>

    <div class="w-[400px]">
      <%= image_tag(club.photo, class: "h-[400px] w-[400px]") %>
    </div>
  </div>

<% end %>

