# == Schema Information
#
# Table name: carts
#
#  id         :uuid             not null, primary key
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the opponent names and add the columns immediately
# below each opponent, per the syntax in the comments below
#
one: {}
# column: value
#
two: {}
# column: value
